version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/appscheck
permissions:
  - object: /home/ubuntu/appscheck/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu